# 📋 Core S3 Management System - 개발 할 일 목록

최종 업데이트: 2025-12-08
현재 완성도: **92%**

---

## 🚀 즉시 실행 (현재 세션) - HIGH PRIORITY

### ✅ 1. 장비 등록 및 테스트

**상태:** 준비 완료
**작업 시간:** 5분
**설명:** 장비 등록 스크립트를 실행하여 Core S3 장비를 시스템에 등록

**실행 방법:**

```powershell
# 가상환경 활성화 (필요시)
.\venv\Scripts\Activate.ps1

# 장비 등록
python backend\register_device.py
```

**확인 사항:**

- [ ] 백엔드 서버 실행 중 (port 8000)
- [ ] MQTT 브로커 실행 중 (port 1883)
- [ ] 스크립트 실행 후 "✅ 장비 등록 성공" 메시지 확인
- [ ] 프론트엔드(localhost:3000)에서 장비 확인

---

## 🎯 단기 목표 (이번 주) - HIGH PRIORITY

### 🔄 2. 스피커 제어 UI 추가

**상태:** 백엔드 완료, 프론트엔드 70% 완료
**작업 시간:** 2-3시간
**중요도:** ⭐⭐⭐⭐

**필요한 이유:**

- 백엔드 API는 완전히 구현됨
- 오디오 재생 기능 완성을 위해 필요
- 사용자가 업로드한 오디오 파일을 장비에서 재생할 수 있어야 함

**작업 내용:**

1. 스피커 제어 컴포넌트 추가
   - 파일: `frontend/src/components/DeviceControl.tsx`
   - 위치: 마이크 제어 UI 아래에 추가
2. 구현할 기능:

   - 재생/일시정지/정지 버튼
   - 볼륨 슬라이더 (0-100)
   - 오디오 파일 선택 드롭다운
   - 현재 재생 상태 표시

3. 백엔드 API 연동:
   ```typescript
   // lib/api.ts에 이미 구현됨
   controlAPI.speaker(deviceId, {
     action: "play" | "pause" | "stop",
     audio_file: "filename.mp3",
     volume: 70,
   });
   ```

**참고 파일:**

- `backend/app/api/control.py` (167-228번 줄) - 스피커 제어 API
- `backend/app/api/audio.py` - 오디오 파일 관리 API
- `frontend/src/components/DeviceControl.tsx` - 기존 제어 UI

**체크리스트:**

- [ ] 스피커 제어 섹션 UI 추가
- [ ] 재생/일시정지/정지 버튼 구현
- [ ] 볼륨 슬라이더 구현
- [ ] 오디오 파일 목록 조회 및 선택
- [ ] API 연동 및 에러 처리
- [ ] Toast 알림 추가
- [ ] 실제 장비에서 테스트

---

### 🔄 3. 장비 등록 UI 추가

**상태:** 미구현
**작업 시간:** 3-4시간
**중요도:** ⭐⭐⭐⭐

**필요한 이유:**

- 현재는 Python 스크립트로만 장비 등록 가능
- 관리자가 웹 UI에서 직접 장비를 등록할 수 있어야 함
- 사용성 개선

**작업 내용:**

1. 대시보드에 "장비 등록" 버튼 추가

   - 파일: `frontend/src/app/dashboard/page.tsx`
   - 위치: 통계 카드 옆 또는 상단

2. 장비 등록 모달/페이지 생성
   - 새 파일: `frontend/src/components/RegisterDeviceModal.tsx`
3. 입력 필드:

   ```typescript
   - device_id: string (필수, 고유값)
   - device_name: string (필수)
   - device_type: 'CoreS3' | 'ESP32CAM' (선택)
   - ip_address: string (필수, IP 형식 검증)
   - location: string (선택)
   - description: string (선택)
   ```

4. 폼 검증:

   - device_id: 영문, 숫자, 언더스코어만 허용
   - ip_address: IPv4 형식 검증
   - 모든 필수 필드 입력 확인

5. API 연동:
   ```typescript
   // lib/api.ts에 이미 구현됨
   devicesAPI.create({
     device_id,
     device_name,
     device_type,
     ip_address,
     location,
     description,
   });
   ```

**UI 디자인:**

```
┌─────────────────────────────────────┐
│  장비 등록                             │
├─────────────────────────────────────┤
│  장비 ID *        [input]             │
│  장비명 *         [input]             │
│  장비 타입        [select]            │
│  IP 주소 *        [input]             │
│  위치             [input]             │
│  설명             [textarea]          │
│                                       │
│         [취소]  [등록]                │
└─────────────────────────────────────┘
```

**체크리스트:**

- [ ] 등록 버튼 추가 (대시보드)
- [ ] 등록 모달 컴포넌트 생성
- [ ] 폼 입력 필드 구현
- [ ] 입력 검증 로직
- [ ] API 연동
- [ ] 성공/실패 처리
- [ ] 등록 후 목록 자동 갱신
- [ ] 에러 메시지 표시

---

### 🔄 4. RTSP 스트리밍 완성

**상태:** 기본 구조 완료, 테스트 필요
**작업 시간:** 4-6시간
**중요도:** ⭐⭐⭐

**필요한 이유:**

- 실시간 영상 모니터링은 핵심 기능
- 카메라 제어와 함께 영상을 볼 수 있어야 함

**작업 내용:**

1. 백엔드 RTSP URL 확인

   - 파일: `backend/app/models/device.py`
   - RTSP URL 생성: `rtsp://{ip_address}:554/stream`

2. 프론트엔드 비디오 플레이어 추가

   - 새 파일: `frontend/src/components/VideoPlayer.tsx`
   - 라이브러리 선택:
     - Option 1: `react-player` (간단)
     - Option 2: `video.js` (고급 기능)
     - Option 3: `hls.js` (HLS 스트리밍)

3. 장비 상세 페이지에 통합

   - 파일: `frontend/src/app/devices/[id]/page.tsx`
   - 카메라 상태 표시 옆에 비디오 영역 추가

4. ESP32 펌웨어 RTSP 서버 확인
   - 파일: `firmware/src/camera_module.cpp`
   - RTSP 서버 라이브러리 확인 및 설정

**기술적 고려사항:**

- RTSP는 브라우저에서 직접 재생 불가
- 변환 필요:
  - Option 1: FFmpeg로 HLS/DASH 변환
  - Option 2: WebRTC로 변환
  - Option 3: RTSP → HTTP MJPEG 변환

**권장 방법:**

```
ESP32 Camera → MJPEG HTTP Stream → Browser
URL: http://{device_ip}:81/stream
```

**체크리스트:**

- [ ] RTSP/HTTP 스트리밍 방식 결정
- [ ] 펌웨어에 스트리밍 서버 구현
- [ ] 프론트엔드 비디오 플레이어 컴포넌트
- [ ] 장비 상세 페이지 통합
- [ ] 재생/정지 제어
- [ ] 에러 처리 (연결 실패, 타임아웃)
- [ ] 실제 장비에서 테스트

---

## 📅 중기 목표 (다음 주) - MEDIUM PRIORITY

### 🔄 5. 인증 시스템 재활성화

**상태:** 구현 완료, 현재 비활성화됨
**작업 시간:** 2-3시간
**중요도:** ⭐⭐⭐⭐⭐

**필요한 이유:**

- 프로덕션 배포 전 필수
- 보안 강화
- 사용자별 권한 관리

**작업 내용:**

#### 5-1. 백엔드 인증 재활성화

수정할 파일: `backend/app/api/devices.py`

```python
# 수정 전 (40번 줄)
@router.get("/", response_model=DeviceListResponse)
async def list_devices(
    # TODO: 로그인 수정 후 활성화
    # current_user: User = Depends(get_current_active_user),
    db: Session = Depends(get_db)
):

# 수정 후
@router.get("/", response_model=DeviceListResponse)
async def list_devices(
    current_user: User = Depends(get_current_active_user),
    db: Session = Depends(get_db)
):
```

수정할 위치:

- `backend/app/api/devices.py`: 40, 75, 99, 273번 줄
- `backend/app/api/control.py`: 32, 98, 167, 230번 줄

#### 5-2. 프론트엔드 인증 재활성화

수정할 파일: `frontend/src/app/page.tsx`

```typescript
// 수정 전 (10번 줄)
useEffect(() => {
  // 임시: 로그인 우회, 바로 대시보드로
  router.push("/dashboard");
}, [router]);

// 수정 후
useEffect(() => {
  const token = localStorage.getItem("access_token");
  if (token) {
    router.push("/dashboard");
  } else {
    router.push("/login");
  }
}, [router]);
```

#### 5-3. 보호된 라우트 가드 추가

새 파일: `frontend/src/middleware.ts`

```typescript
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export function middleware(request: NextRequest) {
  const token = request.cookies.get("access_token");

  if (!token && !request.nextUrl.pathname.startsWith("/login")) {
    return NextResponse.redirect(new URL("/login", request.url));
  }

  return NextResponse.next();
}

export const config = {
  matcher: ["/dashboard/:path*", "/devices/:path*"],
};
```

**체크리스트:**

- [ ] 백엔드 API 인증 체크 활성화 (모든 TODO 제거)
- [ ] 프론트엔드 로그인 우회 제거
- [ ] 라우트 가드 미들웨어 추가
- [ ] 토큰 만료 처리
- [ ] 자동 토큰 갱신 테스트
- [ ] 로그아웃 기능 테스트
- [ ] 권한별 접근 제어 테스트

---

### 🔄 6. 감사 로그 완성

**상태:** 70% 완료
**작업 시간:** 2시간
**중요도:** ⭐⭐⭐⭐

**필요한 이유:**

- 보안 감사 추적
- 문제 발생 시 원인 분석
- ISMS-P 요구사항

**작업 내용:**

1. TODO 주석 제거 및 로그 기록 활성화

   - `backend/app/api/devices.py`: 144-156번 줄
   - `backend/app/api/control.py`: 66-77번 줄

2. 감사 로그 조회 API 추가

   - 새 파일: `backend/app/api/audit.py`

   ```python
   @router.get("/audit-logs/")
   async def list_audit_logs(
       page: int = 1,
       page_size: int = 50,
       user_id: Optional[int] = None,
       device_id: Optional[int] = None,
       action: Optional[str] = None,
       current_user: User = Depends(require_admin),
       db: Session = Depends(get_db)
   ):
       # 감사 로그 조회 로직
       pass
   ```

3. 프론트엔드 감사 로그 페이지 (선택사항)
   - 새 페이지: `frontend/src/app/admin/audit-logs/page.tsx`

**체크리스트:**

- [ ] 백엔드 TODO 주석 제거
- [ ] 모든 관리자 액션에 로그 기록 확인
- [ ] 감사 로그 조회 API 추가
- [ ] IP 주소 추적 확인
- [ ] 민감정보 필터링 확인
- [ ] 로그 보존 기간 설정 (예: 90일)

---

### 🔄 7. 테스트 코드 작성

**상태:** 미구현
**작업 시간:** 8-10시간
**중요도:** ⭐⭐⭐⭐

**필요한 이유:**

- 버그 조기 발견
- 리팩토링 안전성
- 코드 품질 향상

**작업 내용:**

#### 7-1. 백엔드 테스트 (pytest)

새 파일들:

```
backend/tests/
  ├── __init__.py
  ├── conftest.py          # 테스트 설정
  ├── test_auth.py         # 인증 테스트
  ├── test_devices.py      # 장비 관리 테스트
  ├── test_control.py      # 장비 제어 테스트
  └── test_security.py     # 보안 테스트
```

주요 테스트 케이스:

- 인증: 로그인, 토큰 갱신, 권한 체크
- 장비: CRUD 작업, 상태 보고
- 제어: 카메라/마이크/스피커/디스플레이
- 보안: 권한 없는 접근, SQL Injection

의존성 추가:

```bash
pip install pytest pytest-asyncio httpx
```

#### 7-2. 프론트엔드 테스트 (Jest + React Testing Library)

새 파일들:

```
frontend/src/
  ├── __tests__/
  │   ├── components/
  │   │   ├── DeviceCard.test.tsx
  │   │   ├── DeviceControl.test.tsx
  │   │   └── DashboardStats.test.tsx
  │   └── pages/
  │       ├── dashboard.test.tsx
  │       └── login.test.tsx
```

의존성 추가:

```bash
npm install --save-dev jest @testing-library/react @testing-library/jest-dom
```

**체크리스트:**

- [ ] 백엔드 테스트 환경 설정
- [ ] 인증 테스트 (5개 이상)
- [ ] 장비 관리 테스트 (10개 이상)
- [ ] 제어 API 테스트 (8개 이상)
- [ ] 프론트엔드 테스트 환경 설정
- [ ] 컴포넌트 테스트 (주요 컴포넌트)
- [ ] 통합 테스트 (E2E)
- [ ] CI/CD 파이프라인 통합

---

### 🔄 8. 배포 가이드 작성

**상태:** 미구현
**작업 시간:** 4-6시간
**중요도:** ⭐⭐⭐

**필요한 이유:**

- 프로덕션 배포 준비
- 팀원 온보딩
- 문서화

**작업 내용:**
새 파일: `DEPLOYMENT.md`

포함 내용:

1. Windows 배포 가이드

   - IIS 설정
   - Nginx 설정 (선택)
   - 서비스 등록

2. Ubuntu 배포 가이드

   - Nginx + Gunicorn
   - Systemd 서비스
   - 방화벽 설정

3. Docker 배포

   - Dockerfile 작성
   - docker-compose.yml
   - 환경 변수 설정

4. 보안 체크리스트
   - HTTPS 설정
   - 비밀번호 변경
   - 방화벽 설정
   - 정기 백업

**체크리스트:**

- [ ] Windows 배포 가이드
- [ ] Ubuntu 배포 가이드
- [ ] Docker 배포 가이드
- [ ] 환경 변수 템플릿
- [ ] 보안 체크리스트
- [ ] 모니터링 설정
- [ ] 백업/복구 절차
- [ ] 트러블슈팅 가이드

---

## 🔮 장기 목표 (프로덕션 준비) - LOW PRIORITY

### 🔄 9. HTTPS 설정

**작업 시간:** 2-3시간
**중요도:** ⭐⭐⭐⭐⭐ (프로덕션 필수)

**작업 내용:**

1. SSL 인증서 발급

   - Let's Encrypt (무료)
   - 또는 상용 인증서

2. Nginx 리버스 프록시 설정

   ```nginx
   server {
       listen 443 ssl;
       server_name yourdomain.com;

       ssl_certificate /path/to/cert.pem;
       ssl_certificate_key /path/to/key.pem;

       location / {
           proxy_pass http://localhost:8000;
       }
   }
   ```

3. 프론트엔드 URL 업데이트
   ```env
   NEXT_PUBLIC_API_URL=https://api.yourdomain.com
   NEXT_PUBLIC_WS_URL=wss://api.yourdomain.com
   ```

---

### 🔄 10. MQTT TLS/SSL

**작업 시간:** 3-4시간
**중요도:** ⭐⭐⭐⭐ (프로덕션 권장)

**작업 내용:**

1. Mosquitto TLS 설정

   ```
   listener 8883
   cafile /path/to/ca.crt
   certfile /path/to/server.crt
   keyfile /path/to/server.key
   ```

2. 백엔드 MQTT 클라이언트 업데이트

   ```python
   mqtt_client.tls_set(
       ca_certs="ca.crt",
       certfile="client.crt",
       keyfile="client.key"
   )
   ```

3. ESP32 펌웨어 TLS 설정

---

### 🔄 11. Rate Limiting

**작업 시간:** 2-3시간
**중요도:** ⭐⭐⭐⭐ (DoS 방지)

**작업 내용:**

1. FastAPI-Limiter 추가

   ```python
   from fastapi_limiter import FastAPILimiter
   from fastapi_limiter.depends import RateLimiter

   @router.post("/login")
   @limiter.limit("5/minute")
   async def login(...):
       pass
   ```

2. Redis 설치 및 설정
   ```bash
   pip install fastapi-limiter redis
   ```

---

### 🔄 12. 모니터링 시스템

**작업 시간:** 6-8시간
**중요도:** ⭐⭐⭐

**작업 내용:**

1. Prometheus + Grafana
2. 로그 집계 (ELK Stack)
3. 알림 시스템 (Slack, Email)

---

### 🔄 13. 자동 백업

**작업 시간:** 4-6시간
**중요도:** ⭐⭐⭐⭐⭐

**작업 내용:**

1. MySQL 자동 백업 스크립트
2. 백업 파일 암호화
3. 원격 저장소 업로드 (S3, Google Drive)
4. 복구 절차 문서화

---

## 📊 작업 우선순위 요약

### 이번 주 (필수)

1. ✅ 장비 등록 및 테스트
2. 🔄 스피커 제어 UI (2-3시간)
3. 🔄 장비 등록 UI (3-4시간)
4. 🔄 RTSP 스트리밍 (4-6시간)

**예상 총 작업 시간: 9-13시간**

### 다음 주 (중요)

5. 🔄 인증 시스템 재활성화 (2-3시간)
6. 🔄 감사 로그 완성 (2시간)
7. 🔄 테스트 코드 작성 (8-10시간)
8. 🔄 배포 가이드 (4-6시간)

**예상 총 작업 시간: 16-21시간**

### 프로덕션 준비 (선택)

9. 🔄 HTTPS (2-3시간)
10. 🔄 MQTT TLS (3-4시간)
11. 🔄 Rate Limiting (2-3시간)
12. 🔄 모니터링 (6-8시간)
13. 🔄 자동 백업 (4-6시간)

**예상 총 작업 시간: 17-24시간**

---

## 🎯 권장 작업 순서

### Day 1 (현재)

- [x] 프로젝트 구조 파악
- [x] 구현 상태 보고서 작성
- [ ] 장비 등록 스크립트 실행
- [ ] 대시보드에서 장비 확인

### Day 2-3

- [ ] 스피커 제어 UI 구현
- [ ] 장비 등록 UI 구현

### Day 4-5

- [ ] RTSP 스트리밍 완성
- [ ] 실제 장비 테스트

### Week 2

- [ ] 인증 시스템 재활성화
- [ ] 감사 로그 완성
- [ ] 테스트 코드 작성 시작

### Week 3

- [ ] 테스트 코드 완성
- [ ] 배포 가이드 작성
- [ ] 문서 정리

### Week 4 (프로덕션 준비)

- [ ] HTTPS 설정
- [ ] MQTT TLS 설정
- [ ] Rate Limiting
- [ ] 모니터링 시스템
- [ ] 자동 백업

---

## 📌 중요 참고 사항

### 개발 환경 확인

```powershell
# 백엔드
cd backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 프론트엔드
cd frontend
npm run dev

# MQTT 브로커
mosquitto -v

# 펌웨어
cd firmware
platformio run --target upload
platformio device monitor
```

### API 문서

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 초기 계정

- 사용자명: `admin`
- 비밀번호: `Admin123!`

---

**마지막 업데이트:** 2025-12-08
**다음 검토일:** 매일 업데이트
