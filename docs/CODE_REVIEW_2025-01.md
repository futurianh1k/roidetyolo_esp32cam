# 코드 리뷰 보고서 - M5Stack CoreS3 장비 관리 시스템

**작성일:** 2025-01
**프로젝트:** roidetyolo_esp32cam
**리뷰어:** AI Code Reviewer

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [아키텍처 분석](#2-아키텍처-분석)
3. [펌웨어 (ESP-IDF) 리뷰](#3-펌웨어-esp-idf-리뷰)
4. [백엔드 리뷰](#4-백엔드-리뷰)
5. [프론트엔드 리뷰](#5-프론트엔드-리뷰)
6. [보안 분석](#6-보안-분석)
7. [개선 권장사항](#7-개선-권장사항)
8. [다음 단계](#8-다음-단계)

---

## 1. 프로젝트 개요

### 1.1 시스템 구성

| 컴포넌트       | 기술 스택            | 역할                      |
| -------------- | -------------------- | ------------------------- |
| **펌웨어**     | ESP-IDF 5.5.1, C++   | M5Stack CoreS3 장비 제어  |
| **백엔드**     | FastAPI, Python 3.x  | REST API, MQTT, WebSocket |
| **프론트엔드** | Next.js 16, React 18 | 웹 대시보드               |
| **ASR 서버**   | Python (별도)        | 음성인식 처리             |

### 1.2 주요 기능

- ✅ 카메라 스트리밍 (GC0308)
- ✅ 음성인식 (ASR via WebSocket)
- ✅ MQTT 기반 장비 제어
- ✅ 실시간 상태 모니터링
- ✅ 응급 상황 감지 및 알림

---

## 2. 아키텍처 분석

### 2.1 전체 아키텍처

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  M5Stack CoreS3 │────▶│  MQTT Broker    │◀────│   Backend       │
│  (ESP-IDF)      │     │  (Mosquitto)    │     │   (FastAPI)     │
└────────┬────────┘     └─────────────────┘     └────────┬────────┘
         │                                               │
         │  WebSocket (Audio)                           │ REST API
         ▼                                               ▼
┌─────────────────┐                             ┌─────────────────┐
│   ASR Server    │◀────────────────────────────│   Frontend      │
│   (RK3588/PC)   │     Results                 │   (Next.js)     │
└─────────────────┘                             └─────────────────┘
```

### 2.2 장점

- **명확한 역할 분리**: 펌웨어, 백엔드, 프론트엔드 각각 독립적 모듈
- **이벤트 기반 아키텍처**: ESP-IDF EventGroup, MQTT 활용
- **상태 머신 패턴**: DeviceStateMachine으로 상태 관리
- **서비스 레이어 분리**: AudioService, CameraService, ASRService 등

### 2.3 개선 필요 사항

- ⚠️ AXP2101/AW9523 초기화가 application.cc에 직접 구현 → 별도 클래스로 분리 권장
- ⚠️ 하드코딩된 설정값들 → Kconfig 또는 NVS로 이동 권장

---

## 3. 펌웨어 (ESP-IDF) 리뷰

### 3.1 코드 품질

| 항목        | 상태         | 설명                          |
| ----------- | ------------ | ----------------------------- |
| 코드 구조   | ✅ 양호      | 서비스별 분리, 헤더/소스 분리 |
| 에러 처리   | ⚠️ 개선 필요 | 일부 esp_err_t 결과 무시됨    |
| 메모리 관리 | ✅ 양호      | PSRAM 활용, 적절한 버퍼 크기  |
| 로깅        | ✅ 양호      | ESP_LOGI/W/E 적절히 사용      |

### 3.2 주요 파일 분석

#### `application.cc` (메인 애플리케이션)

```cpp
// ✅ 장점: 명확한 초기화 순서
// STEP 1: I2C and Power Management
// STEP 2: Initialize Audio
// STEP 3: Initialize Camera

// ⚠️ 개선점: 전역 변수 사용
static AudioCodec *audio_codec_ = nullptr;  // → Application 클래스 멤버로 이동 권장
```

#### `config.h` (설정 파일)

```cpp
// ❌ 보안 문제: 민감 정보 하드코딩
#define WIFI_SSID "LUKUS"
#define WIFI_PASSWORD "important1@"  // → NVS 또는 Provisioning으로 이동 필요!

#define MQTT_BROKER "10.10.11.18"
#define ASR_SERVER_HOST "10.10.11.17"  // → 설정 가능하게 변경 권장
```

### 3.3 카메라 서비스

```cpp
// ✅ 잘된 점: GC0308 특성 고려
.pixel_format = PIXFORMAT_RGB565,  // JPEG 미지원 카메라용
.xclk_freq_hz = 20000000,          // 정확한 클럭 설정
.sccb_i2c_port = 1,                // 기존 I2C 포트 재사용
```

### 3.4 권장 개선사항

1. **전원 관리 클래스 분리**

```cpp
// 권장: power_manager.h/cc 분리
class PowerManager {
  void InitializeAXP2101();
  void InitializeAW9523();
  void EnableCamera();
  void EnableDisplay();
};
```

2. **NVS 기반 설정 관리**

```cpp
// 권장: WiFi 정보를 NVS에 저장
nvs_get_str(handle, "wifi_ssid", ssid, &len);
nvs_get_str(handle, "wifi_pass", password, &len);
```

---

## 4. 백엔드 리뷰

### 4.1 코드 품질

| 항목      | 상태    | 설명                   |
| --------- | ------- | ---------------------- |
| API 설계  | ✅ 양호 | RESTful, 명확한 라우팅 |
| 보안      | ✅ 양호 | BCrypt, JWT 구현       |
| 에러 처리 | ✅ 양호 | HTTPException 활용     |
| 로깅      | ✅ 양호 | 감사 로그 구현         |
| 테스트    | ⚠️ 필요 | 유닛 테스트 부재       |

### 4.2 보안 구현 분석

#### `security/password.py` ✅ 우수

```python
# BCrypt 12 라운드 - 보안 가이드라인 1-1 준수
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto", bcrypt__rounds=12)
```

#### `security/jwt.py` ✅ 우수

```python
# Refresh Token 해시 저장 - 보안 가이드라인 1-2 준수
def hash_token(token: str) -> str:
    return hashlib.sha256(token.encode()).hexdigest()
```

#### `api/auth.py` ✅ 우수

```python
# 감사 로그 기록 - 보안 가이드라인 9 준수
audit_log = AuditLog(
    user_id=user.id,
    action="user_login",
    ip_address=ip_address
)
```

### 4.3 개선 필요 사항

#### ⚠️ 인증 임시 비활성화

```python
# devices.py - 인증이 주석 처리됨
# TODO: 로그인 수정 후 활성화
# current_user: User = Depends(get_current_active_user),
```

**권장:** 개발 완료 후 반드시 인증 활성화

#### ⚠️ 환경변수 검증 강화

```python
# config.py - 필수 환경변수 검증 추가 권장
SECRET_KEY: str  # 현재: 필수
# 추가 권장:
@validator('SECRET_KEY')
def validate_secret_key(cls, v):
    if len(v) < 32:
        raise ValueError('SECRET_KEY must be at least 32 characters')
    return v
```

---

## 5. 프론트엔드 리뷰

### 5.1 코드 품질

| 항목     | 상태    | 설명                     |
| -------- | ------- | ------------------------ |
| 구조     | ✅ 양호 | App Router (Next.js 13+) |
| 상태관리 | ✅ 양호 | Zustand 사용             |
| API 통신 | ✅ 양호 | Axios + React Query      |
| UI/UX    | ✅ 양호 | Tailwind CSS             |

### 5.2 보안 분석

#### ⚠️ 토큰 저장 방식

```typescript
// authStore.ts - localStorage 사용
localStorage.setItem("access_token", accessToken);
localStorage.setItem("refresh_token", refreshToken);
```

**위험:** XSS 공격에 취약
**권장:** HttpOnly 쿠키 사용 또는 메모리 저장 + Refresh 토큰만 쿠키

#### ⚠️ API URL 노출

```typescript
// api.ts
const API_URL = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
```

**상태:** 정상 - 프론트엔드 환경변수는 노출됨 (민감 정보 아님)

### 5.3 개선 권장사항

1. **토큰 보안 강화**

```typescript
// 권장: 쿠키 기반 인증으로 변경
// 또는 메모리에만 토큰 저장, 새로고침 시 silent refresh
```

2. **에러 바운더리 추가**

```tsx
// 권장: 글로벌 에러 처리
export function ErrorBoundary({ children }) {
  // React Error Boundary 구현
}
```

---

## 6. 보안 분석

### 6.1 보안 가이드라인 준수 현황

| 가이드라인        | 상태        | 설명                              |
| ----------------- | ----------- | --------------------------------- |
| 1-1 비밀번호 해싱 | ✅ 준수     | BCrypt 12 라운드                  |
| 1-2 토큰 관리     | ⚠️ 부분     | Refresh 해시 저장 ✅, 쿠키 미사용 |
| 2 권한 제어       | ⚠️ 부분     | 구현됨, 현재 임시 비활성화        |
| 3 입력 검증       | ✅ 준수     | Pydantic 스키마 사용              |
| 4-1 암호화        | ⚠️ 부분     | JWT 사용, 필드 암호화 없음        |
| 4-2 시크릿 관리   | ⚠️ 개선필요 | 펌웨어 하드코딩 문제              |
| 5 로깅            | ✅ 준수     | 감사 로그 구현                    |
| 9 관리 기능       | ✅ 준수     | Role 기반 접근 제어               |

### 6.2 취약점 요약

| 심각도  | 위치              | 설명                 | 조치              |
| ------- | ----------------- | -------------------- | ----------------- |
| 🔴 높음 | 펌웨어 config.h   | WiFi/MQTT 하드코딩   | NVS 이동          |
| 🟡 중간 | 프론트엔드        | localStorage 토큰    | HttpOnly 쿠키     |
| 🟡 중간 | 백엔드 devices.py | 인증 임시 비활성화   | 활성화 필요       |
| 🟢 낮음 | 펌웨어            | 디버그 I2C 스캔 로그 | 프로덕션에서 제거 |

---

## 7. 개선 권장사항

### 7.1 즉시 필요 (우선순위: 높음)

1. **펌웨어 민감 정보 분리**

   - WiFi SSID/Password → NVS 또는 BLE Provisioning
   - 서버 주소 → NVS에 저장, 웹 설정 페이지 제공

2. **백엔드 인증 활성화**

   - devices.py의 TODO 주석 제거 및 인증 활성화
   - 테스트 완료 후 배포

3. **프론트엔드 토큰 보안**
   - HttpOnly 쿠키로 Refresh Token 이동
   - Access Token은 메모리에만 저장

### 7.2 권장 개선 (우선순위: 중간)

1. **펌웨어 리팩토링**

   - PowerManager 클래스 분리
   - 전역 변수 → 클래스 멤버 이동
   - 에러 처리 강화

2. **테스트 코드 추가**

   - 백엔드 pytest 테스트
   - 프론트엔드 Jest/Testing Library

3. **문서화**
   - API 문서 (Swagger 자동 생성 활용)
   - 아키텍처 다이어그램

### 7.3 장기 개선 (우선순위: 낮음)

1. **OTA 펌웨어 업데이트**

   - 현재 OTA_GUIDE.md 존재, 구현 확인 필요

2. **모니터링/알림**
   - Prometheus/Grafana 연동
   - 이상 감지 알림

---

## 8. 다음 단계

### 8.1 개발 로드맵

```
Phase 1: 보안 강화 (1-2주)
├── 펌웨어 민감정보 NVS 이동
├── 백엔드 인증 활성화
└── 프론트엔드 토큰 보안 강화

Phase 2: 코드 품질 (2-3주)
├── 펌웨어 리팩토링
├── 테스트 코드 작성
└── 문서화 완료

Phase 3: 기능 확장 (4주+)
├── OTA 업데이트 구현
├── 모니터링 대시보드
└── 멀티 디바이스 관리
```

### 8.2 즉시 조치 가능한 작업

1. ✅ 카메라 활성화 완료 (AXP2101 ALDO3)
2. 🔲 config.h 민감정보 제거 및 NVS 이동
3. 🔲 디버그 로그 정리 (I2C 스캔 등)
4. 🔲 devices.py 인증 활성화

---

## 참고 자료

- [Tasmota M5CoreS3.be](https://github.com/arendst/Tasmota/blob/development/tasmota/berry/drivers/M5CoreS3.be) - AXP2101 전원 설정
- [M5Unified Power_Class.cpp](https://github.com/m5stack/M5Unified) - 전원 관리 참고
- [ESP-IDF Programming Guide](https://docs.espressif.com/projects/esp-idf/en/v5.5.1/)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)

---

_이 보고서는 AI 기반 코드 리뷰 도구로 생성되었습니다._
