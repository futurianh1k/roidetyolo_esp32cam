# pytest 설정 파일

[pytest]
minversion = 7.0
addopts = 
    -v
    --strict-markers
    --tb=short
    --capture=no
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: 스모크 테스트
    regression: 회귀 테스트
    ui: UI 테스트
    slow: 느린 테스트
